Owner: Person C (Fatigue, Streaming and Demo)

Plain HTML page, canvas, WebSocket client. No build step, no framework,
no install. Runs from a file. See docs/03-architecture.md, "System overview"
and "D. Stream to laptop".

This folder is separate from mobile/ on purpose — it cannot conflict with
anything the other two are doing.
