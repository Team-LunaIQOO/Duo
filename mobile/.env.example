# Optional override. The app automatically maps Duo/.env through app.config.js.
# Expo embeds EXPO_PUBLIC values in the APK. Use a restricted, short-lived key
# and revoke it after the hackathon.
EXPO_PUBLIC_ANTHROPIC_API_KEY=sk-ant-replace-me
EXPO_PUBLIC_ANTHROPIC_MODEL=claude-haiku-4-5
