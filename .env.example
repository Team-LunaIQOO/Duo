# DEMO ONLY: mobile/app.config.js turns these into EXPO_PUBLIC values, which
# Expo embeds in the APK. Use a short-lived, spend-limited key; never ship it
# outside the hackathon and revoke it immediately afterwards.
ANTHROPIC_API_KEY=sk-ant-replace-me
ANTHROPIC_MODEL=claude-haiku-4-5
